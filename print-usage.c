#include <stdio.h>
#include <stdlib.h>

void print_usage() {
    printf("\n");
    printf("USAGE (option 1) => wbfs-merge-cli [filename.wbfs]\n");
    printf("USAGE (option 2) => wbfs-merge-cli --id [game-id]\n");
    exit(0);
}